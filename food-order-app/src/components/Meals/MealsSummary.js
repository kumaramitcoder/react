import classes from "./MealsSummary.module.css";

const MealsSummary = () => {
  return (
    <section className={classes.summary}>
      <h2>delicious food, Delivered to you</h2>
      <p>
        MakeTypes generates TypeScript classes that parse and typecheck JSON
        objects at runtime, and let you statically type check code that
        interacts with JSON objects. All it requires is a set of JSON samples.
      </p>
      <p>
        MakeTypes generates TypeScript classes that parse and typecheck JSON
        objects at runtime, and let you statically type check code that
        interacts with JSON objects. All it requires is a set of JSON samples.
      </p>
    </section>
  );
};
export default MealsSummary;
